# Everything seer needs is here or in the `kernel` wheel the Makefile builds.
#
# SageMath used to be an optional extra: the modelling - a boolean algebra over
# table variables and a foreign key multigraph - was its `BooleanPolynomialRing`
# and its `Graph`. Both are ports in `kernel` now, so nothing reaches for it and
# nothing reaches for sympy either.
numpy>=1.24.0
sqlglot>=25.0.0
# Read by tests/main.py, which is the corpus half of the test suite:
pyyaml>=6.0
# Required only by the live.py entry point, installed by `make model`:
# llama-cpp-python>=0.2.0
