[project]
name = "seer"
version = "0.1.0"
description = "An SQL syntax graph generator framework based on the kernel library"
requires-python = ">=3.14"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]

[tool.setuptools]
packages = ["src"]
