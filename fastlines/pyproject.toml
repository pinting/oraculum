[build-system]
requires = ["maturin>=1.4,<2.0"]
build-backend = "maturin"

[project]
name = "fastlines"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["numpy"]

[tool.maturin]
bindings = "pyo3"
features = ["pyo3"]
